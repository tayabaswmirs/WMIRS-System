rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      // Allow read and write access if the user is authenticated and matches the folder owner
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
